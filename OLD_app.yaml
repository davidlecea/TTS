runtime: python
env: flex
api_version: 1
threadsafe: true
entrypoint: python main.py

runtime_config:
  python_version: 3

skip_files:
  - ^\.git/.*

# [START handlers]
handlers:
- url: /static
  static_dir: static
- url: /.*
  script: main.app
# [END handlers]